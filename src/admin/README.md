# Admin / submissions backend (placeholder)

This folder is reserved for **staff-only** tools to manage submitted Comprehensive Plan records (list, filter, status, export).

The **user-facing** form lives under `src/components/ComprehensivePlanForm.tsx` and must stay independent so deploys can ship the public app without admin UI.
