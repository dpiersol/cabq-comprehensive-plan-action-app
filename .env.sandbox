# Vite env loaded for `vite build --mode sandbox` / `npm run build:sandbox`.
# This file is committed intentionally so the sandbox build is reproducible.
# DO NOT copy these values into `.env.production` — the dev-login page is
# sandbox-only and must never be shipped to end users.

VITE_DEV_LOGIN_ENABLED=true
